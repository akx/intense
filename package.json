{
  "name": "intense",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "poi --prod",
    "start": "poi --serve",
    "deploy": "rm -rf dist && yarn build --public-url=/intense/ && ghpg -j -d dist -b gh-pages -p"
  },
  "source": "src/index.js",
  "devDependencies": {
    "eslint": "^7.14.0",
    "eslint-config-prettier": "^6.15.0",
    "ghpg": "^2.0.0",
    "poi": "^12.10.3",
    "prettier": "^2.2.0"
  }
}
